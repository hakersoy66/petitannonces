"use client";
import{useState}from"react";
export default function UnsubscribeClient({token}:{token:string}){const[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");async function confirm(){setBusy(true);setError("");try{const r=await fetch("/api/outreach/unsubscribe",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token})});if(!r.ok)throw new Error();setDone(true)}catch{setError("La désinscription n’a pas pu être enregistrée. Réessayez.")}finally{setBusy(false)}}if(done)return <><h1 style={{fontSize:30,margin:"5px 0 12px"}}>Désinscription enregistrée</h1><p style={{lineHeight:1.7,color:"#626276"}}>Cette adresse ne recevra plus de messages de prospection commerciale de Petit Annonces. Elle est conservée uniquement dans notre liste d’opposition afin de respecter durablement votre choix.</p><a href="/" style={{display:"inline-block",marginTop:12,color:"#5b4cf0",fontWeight:800}}>Retour à Petit Annonces</a></>;return <><h1 style={{fontSize:30,margin:"5px 0 12px"}}>Ne plus recevoir nos messages Pro</h1><p style={{lineHeight:1.7,color:"#626276"}}>Confirmez votre choix. La désinscription est gratuite et sera prise en compte immédiatement.</p>{error&&<p style={{color:"#b42318",fontWeight:700}}>{error}</p>}<button type="button" onClick={()=>void confirm()} disabled={busy} style={{border:0,borderRadius:12,background:"#5b4cf0",color:"#fff",fontWeight:900,padding:"14px 20px",cursor:"pointer",opacity:busy?.7:1}}>{busy?"Enregistrement…":"Confirmer la désinscription"}</button><p style={{fontSize:12,color:"#858596",marginTop:22}}>Nous conserverons uniquement l’empreinte nécessaire de l’adresse dans notre liste d’opposition pour éviter toute nouvelle sollicitation.</p></>}
"use client";
import { FormEvent,useEffect,useState } from "react";
import { SiteHeader } from "../../../components/site-header";
import styles from "../engagement.module.css";
function api(){return (process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:4000").replace(/\/$/,"")}
type Search={id:string;name:string;query:string|null;city:string|null;categorySlug:string|null;alertEnabled:boolean;frequency:"INSTANT"|"DAILY"|"WEEKLY"};
export default function SavedSearchesPage(){const [items,setItems]=useState<Search[]>([]);const [form,setForm]=useState({name:"",query:"",city:""});const load=()=>fetch(`${api()}/account/saved-searches`,{credentials:"include"}).then(r=>r.ok?r.json():Promise.reject()).then(p=>setItems(p.searches??[]));useEffect(()=>{load()},[]);async function add(e:FormEvent){e.preventDefault();const r=await fetch(`${api()}/account/saved-searches`,{method:"POST",credentials:"include",headers:{"content-type":"application/json"},body:JSON.stringify({...form,alertEnabled:true,frequency:"DAILY"})});if(r.ok){setForm({name:"",query:"",city:""});load()}}async function update(s:Search,patch:Partial<Search>){const next={...s,...patch};await fetch(`${api()}/account/saved-searches/${s.id}`,{method:"PUT",credentials:"include",headers:{"content-type":"application/json"},body:JSON.stringify(next)});setItems(v=>v.map(x=>x.id===s.id?next:x))}async function remove(id:string){await fetch(`${api()}/account/saved-searches/${id}`,{method:"DELETE",credentials:"include"});setItems(v=>v.filter(x=>x.id!==id))}return <div className={styles.page}><SiteHeader/><main className={styles.shell}><div className={styles.top}><div><span className={styles.eyebrow}>Mon compte</span><h1>Recherches enregistrées</h1><p>Enregistrez vos critères et choisissez quand être alerté des nouvelles annonces.</p></div><span className={styles.count}>{items.length} recherche(s)</span></div><nav className={styles.tabs}><a href="/mon-compte/favoris">Favoris</a><a className={styles.active} href="/mon-compte/recherches">Recherches enregistrées</a><a href="/mon-compte/notifications">Notifications</a></nav><section className={styles.panel}><form className={styles.form} onSubmit={add}><input placeholder="Nom de la recherche" value={form.name} onChange={e=>setForm(v=>({...v,name:e.target.value}))} required/><input placeholder="Mots-clés" value={form.query} onChange={e=>setForm(v=>({...v,query:e.target.value}))}/><input placeholder="Ville" value={form.city} onChange={e=>setForm(v=>({...v,city:e.target.value}))}/><button>Enregistrer</button></form><div className={styles.searchList}>{items.length===0?<div className={styles.empty}>Aucune recherche enregistrée.</div>:items.map(s=><div className={styles.row} key={s.id}><div><h3>{s.name}</h3><p>{[s.query,s.city,s.categorySlug].filter(Boolean).join(" · ")||"Tous les résultats"}</p></div><div className={styles.controls}><label><input type="checkbox" checked={s.alertEnabled} onChange={e=>update(s,{alertEnabled:e.target.checked})}/> Alertes</label><select className={styles.select} value={s.frequency} onChange={e=>update(s,{frequency:e.target.value as Search["frequency"]})}><option value="INSTANT">Instantané</option><option value="DAILY">Quotidien</option><option value="WEEKLY">Hebdomadaire</option></select><a className={styles.button} href={`/recherche?q=${encodeURIComponent(s.query??"")}${s.city?`&ville=${encodeURIComponent(s.city)}`:""}`}>Voir</a><button className={styles.danger} onClick={()=>remove(s.id)}>Supprimer</button></div></div>)}</div></section></main></div>}

"use client";
import { useEffect,useState } from "react";
import { SiteHeader } from "../../../components/site-header";
import styles from "../engagement.module.css";
function api(){return (process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:4000").replace(/\/$/,"")}
type N={id:string;kind:string;title:string;body:string|null;actionUrl:string|null;readAt:string|null;createdAt:string};
export default function NotificationsPage(){const [items,setItems]=useState<N[]>([]);const [unread,setUnread]=useState(0);const load=()=>fetch(`${api()}/account/notifications`,{credentials:"include"}).then(r=>r.ok?r.json():Promise.reject()).then(p=>{setItems(p.notifications??[]);setUnread(p.unread??0)});useEffect(()=>{load()},[]);async function read(id:string){await fetch(`${api()}/account/notifications/${id}/read`,{method:"POST",credentials:"include"});setItems(v=>v.map(x=>x.id===id?{...x,readAt:new Date().toISOString()}:x));setUnread(v=>Math.max(0,v-1))}async function readAll(){await fetch(`${api()}/account/notifications/read-all`,{method:"POST",credentials:"include"});setItems(v=>v.map(x=>({...x,readAt:x.readAt??new Date().toISOString()})));setUnread(0)}return <div className={styles.page}><SiteHeader/><main className={styles.shell}><div className={styles.top}><div><span className={styles.eyebrow}>Mon compte</span><h1>Notifications</h1><p>Messages importants, offres, activité de vos annonces et alertes de recherche.</p></div><div className={styles.actions}><span className={styles.count}>{unread} non lue(s)</span>{unread>0&&<button className={styles.button} onClick={readAll}>Tout marquer comme lu</button>}</div></div><nav className={styles.tabs}><a href="/mon-compte/favoris">Favoris</a><a href="/mon-compte/recherches">Recherches enregistrées</a><a className={styles.active} href="/mon-compte/notifications">Notifications</a></nav><section className={styles.panel}><div className={styles.notificationList}>{items.length===0?<div className={styles.empty}>Aucune notification pour le moment.</div>:items.map(n=><article className={`${styles.row} ${!n.readAt?styles.unread:""}`} key={n.id}><div><span className={styles.kind}>{n.kind}</span><h3>{n.title}</h3><p>{n.body}</p><p className={styles.meta}>{new Intl.DateTimeFormat("fr-FR",{dateStyle:"medium",timeStyle:"short"}).format(new Date(n.createdAt))}</p></div><div className={styles.controls}>{n.actionUrl&&<a className={styles.button} href={n.actionUrl} onClick={()=>{if(!n.readAt)void read(n.id)}}>Ouvrir</a>}{!n.readAt&&<button className={styles.danger} onClick={()=>read(n.id)}>Marquer comme lu</button>}</div></article>)}</div></section></main></div>}

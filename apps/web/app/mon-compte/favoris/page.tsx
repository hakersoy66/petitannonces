"use client";
import { useEffect,useState } from "react";
import { SiteHeader } from "../../../components/site-header";
import styles from "../engagement.module.css";
function api(){return (process.env.NEXT_PUBLIC_API_URL??"http://127.0.0.1:4000").replace(/\/$/,"")}
type Fav={id:string;listingId:string;title:string|null;slug:string|null;priceMinor:number|null;currency:string;city:string|null;categoryName:string;imageUrl:string|null};
export default function FavoritesPage(){const [items,setItems]=useState<Fav[]>([]);const [loading,setLoading]=useState(true);const load=()=>fetch(`${api()}/account/favorites`,{credentials:"include"}).then(r=>r.ok?r.json():Promise.reject()).then(p=>setItems(p.favorites??[])).finally(()=>setLoading(false));useEffect(()=>{load()},[]);async function remove(id:string){await fetch(`${api()}/account/favorites/${id}`,{method:"DELETE",credentials:"include"});setItems(v=>v.filter(x=>x.listingId!==id))}return <div className={styles.page}><SiteHeader/><main className={styles.shell}><div className={styles.top}><div><span className={styles.eyebrow}>Mon compte</span><h1>Mes favoris</h1><p>Retrouvez les annonces que vous souhaitez comparer ou revoir plus tard.</p></div><span className={styles.count}>{items.length} favori(s)</span></div><nav className={styles.tabs}><a className={styles.active} href="/mon-compte/favoris">Favoris</a><a href="/mon-compte/recherches">Recherches enregistrées</a><a href="/mon-compte/notifications">Notifications</a></nav><section className={styles.panel}>{loading?<div className={styles.empty}>Chargement…</div>:items.length===0?<div className={styles.empty}>Aucun favori pour le moment. Ajoutez des annonces avec le bouton cœur.</div>:<div className={styles.grid}>{items.map(x=><article className={styles.card} key={x.id}><div className={styles.image}>{x.imageUrl?<img src={x.imageUrl} alt=""/>:"♡"}</div><div className={styles.body}><h3>{x.title??"Annonce"}</h3><div className={styles.meta}>{x.categoryName} · {x.city??"France"}</div><div className={styles.price}>{x.priceMinor==null?"Prix non défini":new Intl.NumberFormat("fr-FR",{style:"currency",currency:x.currency??"EUR",maximumFractionDigits:0}).format(x.priceMinor/100)}</div><div className={styles.actions}>{x.slug&&<a className={styles.button} href={`/annonce/${x.slug}`}>Voir l’annonce</a>}<button className={styles.danger} onClick={()=>remove(x.listingId)}>Retirer</button></div></div></article>)}</div>}</section></main></div>}
